import { ContactUsPageComponent } from './contact-us-page.component';

describe('ContactUsPageComponent', () => {
  let component: ContactUsPageComponent;

  beforeEach(() => {
    component = new ContactUsPageComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
