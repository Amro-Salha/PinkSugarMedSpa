import { ServicesPageComponent } from './services-page.component';

describe('ServicesPageComponent', () => {
  let component: ServicesPageComponent;

  beforeEach(() => {
    component = new ServicesPageComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
