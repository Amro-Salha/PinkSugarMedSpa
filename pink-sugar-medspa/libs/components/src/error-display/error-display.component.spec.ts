import { ErrorDisplayComponent } from './error-display.component';

describe('ErrorDisplayComponent', () => {
  let component: ErrorDisplayComponent;

  beforeEach(() => {
    component = new ErrorDisplayComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
